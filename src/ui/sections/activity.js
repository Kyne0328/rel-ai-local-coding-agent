// Activity section — Phase 1 shell
// Full implementation (filters, virtualizer, drawer, pause-live) in Phase 2
export function boot(_data) {
  // Phase 1: activity rendering is handled by home.js renderActivity()
  // This boot is a no-op; Phase 2 replaces it with the full implementation
}

// mountActivity exported so Phase 2 router import doesn't break
export function mountActivity(_container) { /* Phase 2 */ }
