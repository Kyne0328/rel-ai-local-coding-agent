export function mountApprovalGates(container) {
  container.innerHTML = '<h3 style="margin:0 0 16px;font-size:15px;">Approval Gates</h3><p style="color:var(--text-muted);font-size:13px;">Configure which actions require explicit approval. Full implementation in Phase 3.</p>';
}
