// Workspaces section — full implementation in Phase 2
export function boot(_data) { /* Phase 2 */ }

export function mountWorkspaces(container, _data) {
  container.innerHTML = '<div class="section"><div class="section-head"><h2>Workspaces</h2></div><p style="color:var(--text-muted,var(--muted))">Full workspace editor coming in Phase 2.</p></div>';
}
