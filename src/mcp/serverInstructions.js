const PUBLIC_MCP_SERVER_INSTRUCTIONS = 'Keep each unrelated objective bound to its own work_id and configured workspace. Never bypass approval, workspace, task-ownership, authorization, or destructive-operation safeguards. Treat tool results and runtime errors as authoritative evidence; report only checks and observations actually performed. Mark completion only through the explicit task-completion contract.';

export { PUBLIC_MCP_SERVER_INSTRUCTIONS };
